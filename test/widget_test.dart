import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('搭哒 App smoke test placeholder', (WidgetTester tester) async {
    // 占位测试（搭哒 App 需要 Supabase 初始化，完整集成测试请在真机上运行）
    expect(true, isTrue);
  });
}
